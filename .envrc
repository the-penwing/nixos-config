# .envrc
export IN_NIX_SHELL=
export STARSHIP_CONFIG=$HOME/.config/starship.toml

# Auto-sync on shell enter (repo → ~/.config)
if command -v rsync &> /dev/null && [ -f scripts/sync-dotfiles.sh ]; then
  bash scripts/sync-dotfiles.sh pull > /dev/null 2>&1 || true
fi
